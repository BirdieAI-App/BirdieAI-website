# Environment variables are loaded from Vercel.
# Run "npm run env:pull" to sync from your Vercel project.
# Set these in Vercel Dashboard: Project Settings > Environment Variables

# Required for backend/API
MONGODB_URI=
OPENAI_KEY=
JWT_SIGNING_SECRET=
FRONTEND_URL=
NEXT_PUBLIC_BACKEND_URL=

# Auth (Google OAuth)
GOOGLE_ID=
GOOGLE_SECRET=
CALLBACK_URL=  # Optional: VERCEL_URL is used when CALLBACK_URL is not set
# For preview deployments: add https://<deployment-url>/call/auth/google/callback to Google Console redirect URIs

# Optional
FRONTEND_URL_WWW=
FRONTEND_URL_ALT=
SITE_URL=

# Note: VERCEL_URL is set automatically by Vercel for deployments.
# For preview deployments, signup/login use same-origin (no extra config needed).
