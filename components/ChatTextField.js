const ChatTextField = ({ message, loadingLatestMessages, handleOnChange, handleOnFocus, submitting, userLimitReached }) => {
    return (
        <div className="fixed bottom-0 w-full bg-white mt-5 flex flex-col justify-center items-center text-center basis-1/6">
              
            </div>
          
    )
}

export default ChatTextField;